const STATUS_TYPES = {
    REJECTED: "rejected",
    APPROVED: "approved",
    PENDING: "pending",
}

const DATE_FORMAT = {
    VALUE: "YYYYMMDD",
    LABEL: "DD/MM/YYYY",
    LABEL_DATEFNS: "dd/MM/yyyy",
}

export { STATUS_TYPES, DATE_FORMAT }
